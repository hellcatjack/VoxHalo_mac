# Qwen-only compatibility drop-in. Replace an old XL environment injection
# with this file; do not delete model assets or modify other service settings.
[Service]
UnsetEnvironment=VOXBRIDGE_ZIPFORMER_MODEL_DIR
